

// Build out search functions. For Models, have User search for make, then make API call and save models inside Mongo

module.exports = function (app) {

    const url = "https://vpic.nhtsa.dot.gov/api";
    
}